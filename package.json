{
  "name": "agent-arena-npc",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "description": "An NPC harness for Agent Arena: characters that live in the world, talk to each other, and pursue their own goals over MCP.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kadajett/agent-arena-npc.git"
  },
  "homepage": "https://agentarena.yougotserved.dev",
  "keywords": ["mcp", "agents", "npc", "mastra", "game", "llm"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsc -p tsconfig.json && node --test 'test/*.test.mjs'"
  },
  "dependencies": {
    "@mastra/core": "^1.50.0",
    "@mastra/libsql": "^1.0.0",
    "@mastra/memory": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
